function finalValueAfterOperations(operations) {
    let X = 0;
    console.log(operations[0].includes("-"));
    return X;
}
;
let operations = ["--X", "X++", "X++"];
console.log(finalValueAfterOperations(operations));
